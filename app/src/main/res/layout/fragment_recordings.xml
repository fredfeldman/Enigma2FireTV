<?xml version="1.0" encoding="utf-8"?>
<!--
  Recordings screen: recording list on the left, detail panel on the right.
  Focusing a recording updates the detail panel instantly.
  Pressing OK on a recording starts playback.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@color/background_dark">

    <!-- ===== Left panel: recording list ===== -->
    <LinearLayout
        android:layout_width="420dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/surface_dark">

        <!-- Toolbar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center_vertical"
            android:background="@color/surface_dark"
            android:orientation="horizontal"
            android:padding="8dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/recordings"
                android:textColor="@android:color/white"
                android:textSize="16sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/btn_refresh"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button"
                android:text="Refresh"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:padding="8dp"
                android:focusable="true" />

        </LinearLayout>

        <!-- Loading spinner -->
        <ProgressBar
            android:id="@+id/loading_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="8dp"
            android:visibility="gone" />

        <!-- Error message -->
        <TextView
            android:id="@+id/tv_error"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/error"
            android:textSize="13sp"
            android:padding="12dp"
            android:visibility="gone" />

        <!-- Empty state -->
        <TextView
            android:id="@+id/tv_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_recordings"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:padding="16dp"
            android:visibility="gone" />

        <!-- Recording list -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_recordings"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingBottom="8dp" />

    </LinearLayout>

    <!-- Vertical divider -->
    <View
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:background="@color/surface_dark" />

    <!-- ===== Right panel: recording details ===== -->
    <LinearLayout
        android:id="@+id/detail_panel"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Hint when nothing focused -->
        <TextView
            android:id="@+id/tv_detail_hint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/recording_detail_hint"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginTop="40dp"
            android:visibility="visible" />

        <!-- Recording title -->
        <TextView
            android:id="@+id/tv_detail_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold"
            android:maxLines="3"
            android:ellipsize="end"
            android:visibility="gone" />

        <!-- Channel name -->
        <TextView
            android:id="@+id/tv_detail_channel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="@color/accent"
            android:textSize="14sp"
            android:visibility="gone" />

        <!-- Date + time + duration -->
        <TextView
            android:id="@+id/tv_detail_datetime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:visibility="gone" />

        <!-- Divider -->
        <View
            android:id="@+id/detail_divider"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:background="@color/surface_dark"
            android:visibility="gone" />

        <!-- Description (scrollable) -->
        <ScrollView
            android:id="@+id/sv_description"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="gone">

            <TextView
                android:id="@+id/tv_detail_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                android:lineSpacingMultiplier="1.4" />

        </ScrollView>

        <!-- Play button hint at the bottom -->
        <TextView
            android:id="@+id/tv_play_hint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Press OK to play"
            android:textColor="@color/accent"
            android:textSize="13sp"
            android:gravity="center"
            android:visibility="gone" />

    </LinearLayout>

</LinearLayout>
